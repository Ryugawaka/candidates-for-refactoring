// типизация, закоментил т.к. повторяется с примером из calculate2.ts

// function getSum(a:number, b:number) {
//   return a + b;
// }

class Calculator2 {
  multiply(a:number, b:number) {
    return a * b;
  }
}
